name: Tests

on:
  push:
    branches:
    - master
  pull_request:
    branches:
    - master

defaults:
  run:
    shell: bash -l {0}

jobs:
  test:
    runs-on: ${{ matrix.os }}-latest

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu, macos, windows]

    steps:

    - name: Checkout
      uses: actions/checkout@v2

    - name: Setup conda
      uses: conda-incubator/setup-miniconda@v2
      with:
        mamba-version: "*"
        auto-activate-base: false
        channels: conda-forge

    - name: Install dependencies
      run: mamba install pip python=3.9 pybind11 nlohmann_json pybind11_json pytest

    - name: Build package
      run: python setup.py sdist bdist_wheel

    - name: Install the package
      run: pip install -vv xjson*.whl
      working-directory: dist

    - name: Run pytest
      run: pytest . -vvv
